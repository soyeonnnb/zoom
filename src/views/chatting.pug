extends base 

block content
    div#welcome
        form(action="") 
            input(placeholder="방 이름" required type="text")
            button 방 입장
        h4 열려있는 방
        ul
    div#room
        h3
        ul 
        form#name 
            input(placeholder="닉네임 설정" required type="text")
            button 닉네임 저장
        form#msg
            input(placeholder="메세지 작성" required type="text")
            button 보내기

block scripts
    script(src="/public/js/chattingApp.js")